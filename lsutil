# maybe, save the list as file in ~/.cliapplist, so user can add their own bookmark
# I think nowadays people have 'evolved' into pattern-recognizers (we don't memorize stuff anymore, we don't do heavy algebra, etc...)

# Hmmm.. sorry I've forgotten most of the coding stuff already, all of those argv and flags whatever
